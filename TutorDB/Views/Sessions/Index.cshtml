@model IEnumerable<TutorDB.Models.Sessions>

@{
    ViewBag.Title = "Index";
}

<h2>Schedule</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Curriculum.CurricName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Tutor.FirstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SessionTopic)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Date)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Curriculum.CurricName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Tutor.FirstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SessionTopic)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Date)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SessionsId }) |
            @Html.ActionLink("Details", "Details", new { id=item.SessionsId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SessionsId })
        </td>
    </tr>
}

</table>
